---
description: Run Dashboard via NPM
---
1. Navigate to the `dm-agent-dashboard` directory.
// turbo
2. Install dependencies if you haven't already.
`npm install`
// turbo-all
3. Run the development server.
`npm run dev`
